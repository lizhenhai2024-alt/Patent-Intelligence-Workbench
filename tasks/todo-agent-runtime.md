# 智能体运行框架（M2）任务清单

- [x] 定义文件解析/校验和演示定义 `library_qa`。
  - 验收：SPEC 表中每条规则都以中文错误提示强制执行；未知字段被拒绝。
  - 验证：`tests/test_agents.py`。
- [x] 模型预设与配置，密钥存入凭据管理器。
  - 验收：下拉框提供 SPEC 中的主流预设（含小米 MiMo）和自定义；按配置区分认证方式（Bearer / `api-key`）；模型名从预设或获取到的模型列表选择，不手填；密钥只存在凭据管理器中。
  - 验证：相关测试（JSON 中无密钥）。
- [x] 证据校验和越界结论扫描。
  - 验收：只有本次工具返回过的公开号才算数；无引用行被标记而不是删除。
  - 验证：`tests/test_agents.py`。
- [x] 运行循环：白名单、步数上限、确认节点、取消和运行日志。
  - 验收：SPEC 中的假模型场景全部符合预期；日志中无密钥。
  - 验证：`tests/test_agents.py`。
- [x] 桌面智能体页面，含常驻发送说明及检索词/最终输出确认节点，不再逐次弹出发送同意框。（自动化测试 `tests/test_agent_desktop.py` 已通过；两个真实模型配置的人工运行待做。）
  - 验收：工作线程不直接调用 Tk；结果中可见被标记的行。
  - 验证：桌面冒烟测试，加两个真实模型配置的人工运行。
- [x] 文档与 ROADMAP 更新。
  - 验收：说明如何编写/修改定义文件，并附校验规则表。
  - 验证：`python scripts/ai_self_audit.py --full`、`git diff --check`。

- [ ] 两个真实模型配置的人工验收（其中一个 MiMo）。
  - 同一问题分别运行，核对引用、取消、错误提示与日志；记录模型、时间和结果。自动化通过不代替此项。
